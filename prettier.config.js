const config = {
    trailingComma: "none",
    tabWidth: 2,
    printWidth: 200, // 增加宽度以避免自动换行
    arrowParens: "avoid", // 减少括号以缩短行长度
    bracketSpacing: false // 减少括号间空格以缩短行长度
};

export default config;
